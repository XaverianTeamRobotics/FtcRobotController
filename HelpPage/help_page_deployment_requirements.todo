TODO:
 - we need to build this to root
 - we need /apk to be static
 - we need /book to be static, for now at least
 - we need /legacy-apk to be static
 - we need to rename the current src directory to smth else, and it needs to be static
 - we need to move the current index to /1.1.0
 - we need a link to the kdoc, it should be /kdoc (again, must be kept static)
 - if we can avoid hydration and just load as a completely normal spa for the docs we should do that, because the prerenderer doesnt support ResizeObserver
 - maybe we do something like i did for my sona portfolio where i have a static folder and all the files in there get put in the root of HelpPage statically